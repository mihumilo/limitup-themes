# 数据备份同步（不依赖任何特定平台）

原来仓库里的备份是**硬编码推到 Gitee** 的，已经全部移除（见文末清单）。
现在换成这套通用方案：**备份目标由配置指定**，可以是本地目录，也可以是任意 git 远端。

## 一、核心设计

| 目标类型 | 做法 | 适用 |
|---|---|---|
| `local` | 生成快照 `<path>/<YYYYMMDD-HHMMSS>/`，并把 `latest/` 更新为最新一份，按 `keep_snapshots` 轮转 | 你自己机器上跑、NAS / 网盘挂载目录 |
| `git` | **每次全新 clone 到临时目录** → 覆盖同步 → commit → push | 任意 git 远端（自建服务 / 其它代码平台） |

两个关键决策，都是踩过坑后的选择：

**① 为什么 git 目标要"全新 clone"而不是"加个 remote 再 push"**

GitHub Actions 的 `actions/checkout` 默认是**浅克隆**（`fetch-depth: 1`）。
浅仓库推到别的远端会被直接拒绝：

```
! [remote rejected] HEAD -> main (shallow update not allowed)
```

——这正是日志里 Gitee 同步失败的那一行。全新 clone 从根上绕开：本地没有历史包袱，
推的就是一个完整仓库。

**② 为什么 `_manifest.json` 里不能有时间戳**

git 目标用 `git diff --cached --name-only` 判断"有没有变化"。
清单里带个每次都变的时间戳 → 每次都被判定为"有更新" → 每次提交一版空提交。
时间信息由快照目录名（local）和 commit message（git）承载就够了。

## 二、配置

配置文件：仓库根 **`backup.config.json`**（可用 `--config` 指定其它路径）。

```jsonc
{
  "enabled": false,                     // 总开关。false 时脚本直接返回 0，什么都不做
  "include": ["themes", "review", "nontrading-days.json"],
  "targets": [
    {
      "name": "local-nas",              // 目标名（--only 用；日志里显示）
      "type": "local",
      "enabled": false,                 // 单个目标开关
      "path": "D:/backup/limitup-themes",
      "keep_snapshots": 7               // 保留最近 N 份快照
    },
    {
      "name": "mirror-git",
      "type": "git",
      "enabled": false,
      "url": "https://oauth2:${BACKUP_GIT_TOKEN}@git.example.com/你的账号/limitup-themes.git",
      "branch": "main",
      "user": "backup-bot",
      "email": "backup-bot@example.com"
    }
  ]
}
```

**凭据一律走环境变量**：字符串里的 `${VAR}` 会被替换成同名环境变量。
取不到就跳过该目标并告警 —— **绝不会带着空 token 去请求**。
日志里打印 URL 时会自动把 `user:pass@` 打码成 `user:***@`。

> `backup.config.json` 会提交到公开仓库，**只放路径/分支这类非敏感信息**。

### 启用步骤（以 git 远端为例）

1. 编辑 `backup.config.json`：
   - `enabled` → `true`
   - 目标 `mirror-git`：`enabled` → `true`，`url` 换成你的备份远端
2. GitHub 仓库 → **Settings → Secrets and variables → Actions** → New repository secret
   - 名字 `BACKUP_GIT_TOKEN`，值 = 该远端的访问令牌
3. 提交推送。`daily.yml` 里已有一个「数据备份同步」步骤会自动带入该 Secret（**不用改工作流**）

只配本地目录的话，第 2 步可省。

## 三、运行

```bash
# 本地手动跑（最常用）
python scripts/backup_sync.py

# 只看计划，不实际执行
python scripts/backup_sync.py --dry

# 只跑某一个目标
python scripts/backup_sync.py --only mirror-git

# 排障：打印 git 命令与输出
python scripts/backup_sync.py --verbose
```

在 GitHub Actions 上由 `daily.yml` 的「数据备份同步」步骤自动执行（16:30 / 18:00 各一次），
该步骤 `continue-on-error: true` —— **备份失败不会影响主流程**。

> ⚠️ 在 Actions 上跑 `local` 目标时，写的是**临时 runner 的磁盘**，作业结束就丢弃。
> 脚本会主动 WARN 提示；需要留存请用 `git` 目标，或在自己机器上跑脚本。

## 四、日志与结果反馈

- **控制台**：`[时间] 级别 消息`，级别 = `INFO / OK / WARN / ERR`，实时 flush
- **结果文件**：`_work/backup_result.json`（时间、每个目标的成败与说明、完整日志）
- **GitHub 作业摘要**：检测到 `GITHUB_STEP_SUMMARY` 时，追加一张 Markdown 表格
  （在 Actions 作业页直接能看到哪个目标成功、哪个失败、为什么）

**失败隔离**：每个目标独立 try/except，一个失败不影响其它目标，最后统一汇总。

| 情况 | 退出码 |
|---|---|
| 全部成功 / 没有启用任何目标 / 功能关闭 | `0` |
| 至少一个目标失败 | `1` |

## 五、常见错误与处理

| 日志 | 原因 | 处理 |
|---|---|---|
| `缺少环境变量：BACKUP_GIT_TOKEN` | Secret 没配或名字不对 | 仓库 Settings → Secrets 配 `BACKUP_GIT_TOKEN` |
| `克隆失败 … Could not read from remote repository` | URL 错 / 令牌没权限 / 网络不通 | 换 `--verbose` 看 git 原话 |
| `推送失败（检查凭据权限 / 分支保护）` | 令牌只有读权限，或分支被保护 | 给令牌写权限；或换分支 |
| `远端已是最新，无需提交` | 正常（幂等，没有产生空提交） | — |
| `type 未知` | 配置里 `type` 写错 | 只能是 `local` 或 `git` |

## 六、本次改造涉及的文件

**新增**
- `scripts/backup_sync.py` —— 备份同步主程序（仅用标准库，无新依赖）
- `backup.config.json` —— 配置文件（默认 `enabled: false`）

**修改**
- `.github/workflows/daily.yml` —— 新增「数据备份同步」步骤（传入 `BACKUP_GIT_TOKEN`）

**移除（Gitee 相关，全部删除）**
- `.github/workflows/daily.yml` 的 job 级 `GITEE_USER/GITEE_TOKEN` env 与「同步到 Gitee 镜像」步骤
- `.github/workflows/cleanup.yml` 的同样两部分
- `.github/workflows/backfill.yml` 的同样两部分
- `worker.themes.js` 注释里以 Gitee 举例的 `THEME_BASE_EXTRA` 说明（改为通用 CDN 示例）

**需要你在 GitHub 网页上手动删的 Secrets**
- `GITEE_USER`、`GITEE_TOKEN`（Settings → Secrets → 删除）
- 新增：`BACKUP_GIT_TOKEN`（只有用 git 目标时才需要）
