# 回填数据 vs 同花顺真实数据 · 差异核查报告

核查时间：2026-09-22 00:00 · 对比人：自动化核查

## 一、对比基准与范围

| 数据源 | 说明 |
|---|---|
| 回填数据 | 本仓库 `themes/20260921.json`（官方复盘长图 OCR 产物，103 只） |
| 同花顺涨停池 API | `dataapi/limit_up/limit_up_pool`，**只返回"当前交易日"**，不支持历史日期查询 |
| 官方复盘长图 | 圈子 `t.10jqka.com.cn/469885108/` 2026-09-21 帖（pid 696104724），3 张图（1248px 宽） |

**范围限制**：同花顺 API 的历史日期查询实测返回空（带 `date` 参数与不带均如此，深夜接口已切到下一交易日）。因此**逐字段精确对比只能在 20260921 当天**进行（用的是 9/21 17:01 抓取的当日盘后快照，101 只）；其余 29 天做的是**内部完整性检查**（第二节）。

## 二、数据完整性（20260921 + 全部 30 天）

**集合对比（20260921）**：

| 项 | 数值 |
|---|---|
| 回填数据 | 103 只（total=103，poolSize=101，bse=2） |
| 同花顺 API | 101 只 |
| 仅回填有 | 2 只：920427 华维设计、920478 始一药业 —— **都是北交所股** |
| 仅同花顺有 | **0 只**（无遗漏） |

→ 差异原因：API 的 `filter=HS,GEM2STAR` **不含北交所**；官方复盘图含北交所。**回填数据更完整**，bse=2 恰好就是这 2 只北交所。

**内部完整性（全部 30 天）**：

- 30/30 天的 `total == poolSize + bse == 实际个股数`，全部成立
- 3 只个股缺 `time` 字段：**全部是 920 开头北交所股**（920344 / 920223 / 920478），图上北交所行的封板时间未识别到；keyword 亦混入少量正文碎片
- 2 天 `verified=false`（20260901、20260902）→ 根因与修复见第四节

## 三、字段数值一致性（20260921，101 只共同个股）

| 字段 | 差异数 | 结论 |
|---|---|---|
| 名称 | **0** | 完全一致 |
| 涨停原因（keyword vs reason_type） | **0** | 逐字一致 |
| 封板时间 | 53 → **0** | 见下方说明 |
| 连板高度 | 9 → 8 处口径差 + **1 处真缺陷** | 见下方说明 |

### 3.1 封板时间：53 处差异是「字段语义」不同，不是数据错误

用 API 的 `first_limit_up_time`（**首次**封板时间）对比时 53 只不一致，且**全部是"回填更晚"、无一反向**。原因：

- 官方复盘图的列叫「**最终涨停时间**」（炸板再封后最后一次封住的时间）
- API 的 `first_limit_up_time` 是当天**首次**触板时间
- 炸板再封的股票（如华仁药业 首次 10:38 → 最终 13:06）两者必然不同

改用 API 的 `last_limit_up_time`（最终涨停时间）重新对比：**101/101 完全一致，0 差异**。

### 3.2 连板高度：9 处差异逐一看图核实

官方图「连板天数」列是**三种格式混合**：`N连板`（纯连续）/ `N天M板`（断板股）/ `首板`。
API 的 `high_days` 是"近 N 日 M 板"概括，`high_days_value` 可解码为 `(M<<16)|N`。
**两种口径不同源，数值可以合法地不同**：

| 股票 | 回填 | 官方图（看图核实） | THS API | 判定 |
|---|---|---|---|---|
| 双星新材 | 4 | **7天4板** | **首板** | ★ API 与官方图直接矛盾 |
| 美盈森/海洋王/新亚制程/华脉科技/福莱蒽特/豪尔赛 | 1 | **首板** | 4天2板 | API 口径（近4日2板），图上今日连续=1 |
| 大连圣亚 | 2 | **3天2板** | 3天2板 | 一致（回填提取板数 M=2） |
| 华英农业 | 2 | （同上类型） | 3天2板 | 一致 |
| 会稽山 | 4 | **5天4板** | 5天4板 | 一致 |
| 奥康国际 | 2 | **2连板** | **null** | API 数据缺失，回填正确 |
| **百花医药** | **1** | **3天2板** | 3天2板 | **★ 回填唯一真缺陷** |

**百花医药（600721）**：图上竖排"3天2板"清晰可见，回填 streak=1 —— 该行的连板列 OCR 未成功提取，**回落到了涨停池的"今日连续板数"=1**。同类"3天2板"的大连圣亚/华英农业正确提取了 2。属于 OCR 偶发失败 + 回落值语义不一致（M=板数 vs 池值=今日连续），影响 1/103。

> 影响评估：看板页面的梯队板高**优先取当日涨停池 KV（daily_.lu）**，不用 themes JSON 的 streak，页面显示不受此影响。

## 四、发现并已修复的问题

### 4.1 verified 校验容差设计错误（已修）

`pipeline.py` 原校验：

```python
verified = ... and bse <= 2     # bse = 池外股数量
```

设计假设是"池外 = OCR 误识别 ≤2"，但实测 30 天里**全部 21 只池外股无一例外都是 920 开头北交所**（fuyao 池不含北交所，图上有 → 必然落池外）。北交所活跃日池外 3+ 只 → 20260901/20260902 被误判 `verified=false`。

修复：池外股**全部为北交所前缀（92/83/87/43）→ 通过**；出现非北交所前缀的池外股（那才是 OCR 误识别）才判 false。

对 20260901/20260902 做了**确定性规则重放**（非重新 OCR：`bse == 北交所股数 且 非北交所池外 = 0` 可从现有数据直接推导）→ 两天 verified 已更新为 true。

### 4.2 回归测试的仿真构造器行高缺陷（已修）

`test_parse.py` 规模仿真用固定 420px 行距反向构造坐标，keyword ≥6 段的行会越过与下一行的中线（最后一 155px），导致行归属漂移、2 处断言失败。真实图上行高随内容自适应。已改为 `y += 200 + max(0, len(parts)*40 - 160)`。修后自测**全部通过**（17 项 OK）。

## 五、结论

1. **回填数据质量良好**：与同花顺当日真实数据在名称、涨停原因、封板时间（最终口径）上 **101/101 完全一致**；集合上比 API 更完整（多 2 只北交所）。
2. **连板的 9 处差异中 8 处是 THS API 与官方图的口径/数据差异**（API 甚至把图上"7天4板"的双星新材标成"首板"）——官方图才是权威源，回填忠于图。
3. **唯一数据缺陷**：百花医药 streak=1（应为 2），OCR 偶发失败回落池值，影响 1/103，页面显示不受影响。
4. **verified 误判已修**（bse≤2 → 池外全北交所即通过），20260901/02 已重放为 verified=true。
5. 后续如需全量 30 天对官方图的逐字段校验，需 OCR 环境（Actions 上可用 `--force` 重跑任一日期）。
