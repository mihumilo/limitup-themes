# limitup-themes × Worker · 联动审查报告

> 审查时间：2026-09-21　仓库：`C:\Users\Admin\Documents\GitHub\limitup-themes`（origin/main，工作区干净）
> Worker：`C:\Users\Admin\WorkBuddy\涨停复盘\worker.themes.js`（BUILD v2026-09-21.8）

---

## 一、调用关系与数据流转链路

### 1.1 两条独立链路（容易混淆，必须分清）

```
【链路 A · 官方主题分类】—— 本仓库产出，Worker 消费
  GitHub Actions (daily.yml, 北京 16:30 / 20:00)
    → pipeline.py 抓同花顺复盘帖 → 下载长图 → OCR → 解析 → 交叉校验
    → 写 themes/<日期>.json + review/<日期>.json → git push
    → （可选）数据备份同步（backup_sync.py，目标由 backup.config.json 指定）
                                    ↓ 按需拉取（无定时器）
  Worker loadThemes()  ← KV themes_<日期>（30 天）← raw.githubusercontent / jsDelivr
                                    ↓
  页面：今日/昨日概念驱动板块、梯队板块名官方优先、板块跟踪

【链路 B · 当日行情快照】—— 与 GitHub 无关
  Worker cron（CF 控制台配置）→ runScheduledPersist()
    → 同花顺涨停池 fuyao.aicubes.cn（要 X-api-key）
    → 东财炸板/跌停池 push2ex.eastmoney.com
    → 落 KV daily_<日期>
                                    ↓
  页面：涨停/跌停/炸板池、连板梯队、涨跌幅

【反向旁路】GitHub 出口被封时的备用图源
  daily.yml 传 vars.REVIEW_API_BASE → Worker /api/review → pipeline.py 用它抓帖抓图
```

**结论**：两条链路**在正常情况下互不依赖**。唯一耦合点是旁路 `REVIEW_API_BASE`。

### 1.2 ★ 当前断点（P0，必须立即处理）

| # | 断点 | 证据 | 影响 |
|---|---|---|---|
| **B1** | **`080d14e delete old data` 把 30 个 `themes/` + 30 个 `review/` 全删了** | `git show --stat 080d14e`；`raw.githubusercontent.../themes/20260918.json` → **404** | Worker 拉不到任何官方主题 → 概念驱动板块只能走**盘中估算**；梯队板块名的"官方优先"失效；板块跟踪无官方数据。**整条链路 A 目前是断的** |
| **B2** | Worker KV 里可能**还存着删除前的旧数据**（`themes_<日期>` 缓存 30 天） | `KV_PREFIXES` 含 `themes_`，TTL 2592000 | 回填完成后 Worker 仍可能读到**修复前的坏数据**（如 20260812 那个 49 只挤一起的版本） |
| **B3** | Worker 只有 2 个内置源，额外备源需手动配 `THEME_BASE_EXTRA` | `THEME_BASES` 只有 raw + jsDelivr | 未配时行为不变；配了才生效 |

### 1.3 逻辑缺陷（P1）

| # | 问题 | 为什么危险 |
|---|---|---|
| **D1** | **"官方帖还没发布"会被误判成"非交易日"**：`backfill_gaps.py` 的候选包含**今天**，16:30 那次跑时帖子常还没发 → 失败 +1；20:00 再失败 → 满 2 次 → **今天被永久写进 `nontrading-days.json.dates`**。哪怕半小时后帖发布了，也**再也不会补** | 这是"每天丢一天数据"的隐形漏斗 |
| **D2** | `tries` 永不清零：一旦累计到 2，除非手动编辑 JSON，永远不会重试 | 一次网络故障/bug 会让某天永久缺失（20260812 已累计 1 次） |
| **D3** | `test_parse.py` **没有任何 CI 会跑** —— 只在本地手跑 | parser 改坏了要等到产出数据异常才发现（这次宽度 bug 就是这么潜伏的） |

### 1.4 配置不一致（P2）

| # | 不一致 | 修法 |
|---|---|---|
| **C1** | ~~`backfill.yml` 缺 Gitee 同步~~ | 已随 Gitee 整体移除而消除 |
| **C2** | `cleanup.yml` 的 `git push` **无重试**（daily/backfill 都有 3~5 次重试） | 已补 |
| **C3** | Worker 注释描述与实现不符（声称有第三个源，实际只有 2 个） | 已改注释；并新增 `THEME_BASE_EXTRA` 支持追加备源 |
| **C4** | `env.THEME_BASE` 是**替换**整个源列表，无法"追加" | 新增 `THEME_BASE_EXTRA`（追加，支持逗号分隔） |
| **C5** | `cleanup.yml` 用 `git add -A themes review`（范围过大） | 保留（只涉及两个数据目录，风险可控），但已加 `continue-on-error` 与重试 |

### 1.5 重复触发检查

**结论：没有真正的重复触发。** 逐个核对：

- daily 16:30 / 20:00 两次 → 20:00 靠 `run_one` 的"已 verified 就跳过"实现"拿到就不跑第二次" ✅
- daily 的"补跑缺失日期"每次运行都跑，20:00 那次多数日期已存在 → 只是一次文件读判断，无副作用 ✅
- 今天可能被处理两次（补跑 + 转录各一次）→ 第二次被幂等跳过，无副作用 ✅
- cleanup（北京 09:00）与 daily（16:30 / 20:00）时间不重叠 ✅

---

## 二、端到端自动化评估

### 已自动化 ✅

| 环节 | 状态 |
|---|---|
| 每日两次转录（16:30 / 20:00） | 自动 |
| 拿到即停 / 两次拿不到就算了 | 自动（幂等 + 无告警） |
| 缺失日期自动补跑 | 自动（`--keep 30 --max 6`） |
| 历史 JSON 超量自动清理 | 自动（cleanup.yml，按实际文件数保留 30） |
| 提交与推送（含重试） | 自动 |
| 数据备份同步 | 自动（`backup_sync.py`；目标由 `backup.config.json` 指定，默认关闭） |
| Worker 行情快照 | 自动（cron，需在 CF 控制台配） |
| Worker 取官方主题 | 自动（按需 + KV 缓存 + 负缓存） |

### 仍需人工介入 ❌

| # | 环节 | 原因 | 能否消除 |
|---|---|---|---|
| **M1** | **Worker 的两个 cron 触发器** | Cloudflare 的 cron 只能在其控制台配置，代码/wrangler 管不到（本仓库也无 wrangler.toml） | 一次性，配完永久生效 |
| **M2** | **`delete old data` 后的全量重建** | 删除是人工操作，回填虽能自动补（6 天/次、12 天/天，约 3 天补满），但**建议手动触发 backfill.yml 一次跑完** | 一次性 |
| **M3** | **回填后清 Worker KV** | KV 里可能留着旧数据，需后台"清 KV 并重拉"或 `/api/themes?force=1` | 一次性 |
| **M4** | 备份目标的凭据（如 `BACKUP_GIT_TOKEN`） | 需在仓库 Settings 配 | 一次性（不用 git 目标则不需要） |
| **M5** | `nontrading-days.json` 误判修正 | 现在可用 `--reset-tries` 一键清空（原来只能手改 JSON） | 已大幅改善 |
| **M6** | Worker 部署 | 手动粘贴到 CF | 一次性 |

**一句话结论**：稳态运行是全自动的；当前处于**非稳态**——数据被清空了，需要 M2 + M3 两步人工动作拉回正轨。

---

## 三、已实施的修改（文件均已写入磁盘，可直接 commit & push）

| 文件 | 类型 | 作用 | 影响范围 |
|---|---|---|---|
| `scripts/backfill_gaps.py` | **修改** | ① 新增宽限期 `--grace-days`（默认 3 天）：距今不足 3 天的日期失败**只计数、绝不判定为非交易日** → 修掉 D1；② 新增 `--reset-tries` → 修掉 D2 | 只影响补跑的判定逻辑，不改变正常补跑行为 |
| `nontrading-days.json` | **替换** | 清空 `dates` 与 `tries`（原 `tries.20260812=1`） | 让所有日期重新参与补跑 |
| `.github/workflows/backfill.yml` | **修改** | ① 新增 `reset_tries` 输入 + prepare 里的清空步骤；② 移除 Gitee 同步 | 新增输入不影响既有调用 |
| `.github/workflows/cleanup.yml` | **修改** | 提交步骤加 `continue-on-error` + 推送重试 | 修掉 C2；降低并发推送失败导致的"清理没生效" |
| `.github/workflows/daily.yml` | **修改** | 新增「解析回归自测」步骤（continue-on-error） | 修掉 D3；失败只告警不判红 |
| `.github/workflows/test.yml` | **新增** | push/PR 改 `scripts/**` 时立即跑自测 + 编译检查 | 新增工作流，不影响既有流程 |
| `worker.themes.js` | **修改** | ① 修正过时注释（C3）；② 新增 `THEME_BASE_EXTRA` 追加备源（C4） | 未配该变量时行为完全不变 |

> `pipeline.py` / `check_output.py` / `test_parse.py` / `requirements.txt` **本次未改动**
> （上一轮的宽度修复与体检退出码修复已在其中，且与远端一致）。

---

## 四、立即要做的事（按顺序）

1. **回填数据**：Actions → **批量回填历史** → Run workflow
   - `start_date` = `20260811`，`end_date` = `20260921`
   - `shards` = `8`
   - `force` = ❌ 不勾（文件都没了，不会被幂等跳过，勾了反而多花时间）
   - `reset_tries` = ✅ 勾（清空失败计数）
2. **回填完成后清 Worker KV**：看板 → 设置 → 清理 `themes_` 与 `themes_miss_` 前缀
   （或对关键日期访问 `/api/themes?date=YYYYMMDD&force=1`）
3. **（可选）启用额外取数备源**：Worker 环境变量加
   `THEME_BASE_EXTRA = https://<你的镜像域名>/limitup-themes/themes/`（追加在内置源之后）
4. **确认 Worker cron**：Cloudflare 控制台 → Worker → 触发器 → Cron
   `30 8 * * *` 与 `0 12 * * *`（UTC，= 北京 16:30 / 20:00）
5. commit & push 本轮改动


---

## 四之二、Gitee 已彻底移除，备份改为通用方案

按你的要求，项目中所有 Gitee 相关内容已删除（同步步骤 / 远程地址 / 鉴权 / 配置项 / 文档注释），
**全仓检索 `GITEE` 零残留**。删除后已确认：4 个 workflow YAML 解析通过、脚本全部编译通过、
回归自测全绿 —— 无死代码、无残留调用。

新方案：`scripts/backup_sync.py` + `backup.config.json`，**不依赖任何特定平台**，
目标可以是本地目录或任意 git 远端。详见 **[备份同步说明.md](备份同步说明.md)**。

要点：
- git 目标**每次全新 clone**，避开「浅克隆推到别的远端被拒」（`shallow update not allowed`）
  —— 这正是日志里 Gitee 同步失败的那一行
- 凭据走环境变量 `${BACKUP_GIT_TOKEN}`，日志里 URL 自动打码
- 每个目标独立 try/except，失败互不影响；结果写 `_work/backup_result.json` + Actions 作业摘要
- 幂等：内容没变就提示「远端已是最新」，不产生空提交

**需要你手动做的**：GitHub 仓库 Settings → Secrets 里删掉 `GITEE_USER` / `GITEE_TOKEN`。


---

## 五、文件结构清理建议

### 5.1 仓库本体（`Documents\GitHub\limitup-themes`）— 已经很干净

```
.github/workflows/  backfill.yml cleanup.yml daily.yml test.yml(新)
scripts/            backfill_gaps.py check_output.py pipeline.py test_parse.py
DISCLAIMER.md  nontrading-days.json  requirements.txt
themes/.gitkeep  review/.gitkeep
```

**无需删除任何文件。** 两点建议：
- `themes/.gitkeep`、`review/.gitkeep` **必须保留** —— 数据被清空后靠它们保住目录，
  否则 `pipeline.py` 的 `os.makedirs` 虽然会重建，但首次 clone 的目录结构会不稳定。
- **仓库缺 `README.md`**（只有 DISCLAIMER.md）。远端 README 是空的，
  建议在 GitHub 网页或本地补一份，说明数据格式、工作流、如何手动回填。

### 5.2 WorkBuddy 工作目录（`WorkBuddy\涨停复盘`）— 冗余集中在这里

整个目录 **591 MB**，构成：

| 项 | 体积 | 性质 | 删除风险 |
|---|---|---|---|
| `_img/` | **520 MB** | 官方长图原件（早期 OCR 调试下载） | **低**：图 URL 保存在 `review/*.json`（虽已从仓库删除，但**在 git 历史里**），需要时可重新下载。**建议删** |
| `zydl_lhb/` | 39 MB | 龙虎榜席位年度复盘（独立成果） | **不要删**，是另一个项目的产出 |
| `limitup-themes/_work/` | 7.0 MB | OCR 中间产物（`ocr_*.json`） | **低**：每次跑都会重新生成；`.gitignore` 已排除。**建议删** |
| `_thslice/` | 8.7 MB | 长图切片（调试用） | **低**，建议删 |
| `_thimg/` | 4.6 MB | 长图（调试用） | **低**，建议删 |
| `limitup-themes/data/` | 499 KB | 历史工作副本（299 个 review + 13 个 themes），**从未推送过** | **低**：与发布链路无关（Worker 读的是远端 `themes/`）。注意里面 `data/index.json` 可能是早期索引，若以后还要参考就先备份 |
| `_pool*/` | 1.7 MB | 涨停池 JSON 快照 | **低**，建议删 |
| `_tmp*.png`（17 个） | ~4 MB | 一次性探测 | **低**，建议删 |
| `_shots/` | 928 KB | 真机验证截图（有交付价值） | **建议保留** |
| `_verify_tmp/` | 604 KB | 校验脚本临时目录 | **低**，可删 |
| 其余 `_*.js/_*.txt` 共 118 项 | 小 | 早期一次性调试脚本 | **低**，但见下 |

**必须保留（校验工具链，删了以后改代码就没有安全网）**：
`_verify_worker.js`（36 项）、`_verify_render.js`（142 项）、`_audit.js`（死代码/CSS/主题/对比度）、
`_profile.js`、`_mkpreview.js`、`_serve.js`、`_shots.js`

**删除前建议**：`_img/` 占 520MB 是绝对大头，删它就够了；其余加起来不到 25MB，
收益有限但也没风险。删除会进回收站（`genie-trash`），**要真正释放空间必须再清空回收站**。

### 5.3 无用依赖 / 过时配置

- `requirements.txt` 四项全部在用：`requests`（抓帖/池）、`pillow`（读图宽+切图）、
  `numpy<2`（rapidocr 依赖）、`rapidocr-onnxruntime`（OCR）。**无冗余**。
  唯一代价是每次 Actions 装包约 1~2 分钟，已用 `cache: pip` 缓解。
- **没有 `wrangler.toml`** —— Worker 配置全在 Cloudflare 控制台。
  不算"过时配置"，但意味着**代码里看不到 Worker 的 cron 与环境变量**，
  建议把当前配置记进 README（本轮已把 `THEME_BASE_EXTRA` 的用法写进源码注释）。
